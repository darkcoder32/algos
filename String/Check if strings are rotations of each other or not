Complexity Depends upon Implementation of find if it is implemented by kmp then complexity is O(m+n);

bool areRotations(string s1,string s2)
{
    // Your code here
    if(s1.length()!=s2.length()){
        return false;
    }
    s2+=s2;
    return (s2.find(s1)!=string::npos);
}
